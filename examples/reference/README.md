# Reference Audio Examples

Do not commit private character recordings or generated private audio here.

For a local experiment, use a private source file and build candidates with:

```bash
./command/reference build --input <SOURCE_AUDIO> --output <REFERENCE_DIR> --preset conservative
./command/reference analyze <REFERENCE_DIR>/reference_conservative.wav
```

The public repository should contain only synthetic or explicitly redistributable example material.
