#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
echo "Repository: $ROOT"
echo "Copy config/machine.example.env to config/local.env and configure the local Qwen3-TTS environment."
