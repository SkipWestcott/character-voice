# Workflows

Workflow files are optional.

If a workflow is published, it must contain only generic nodes, paths, text,
and settings. Remove private character information and machine-specific paths
before committing.
